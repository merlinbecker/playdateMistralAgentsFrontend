{
    "version": "0.2.0",
    "configurations": [
        {
            "request": "launch",
            "type": "playdate",
            "name": "Run app in Playdate simulator",
            "source": "${workspaceFolder}/Source",
            "output": "${workspaceFolder}/mygame.pdx",
            "sdkPath": "C:/code/PlaydateSDK"
        }
    ]
}
